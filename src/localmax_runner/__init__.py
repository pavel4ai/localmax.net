"""LocalMax benchmark runner.

Runs a pinned benchmark profile against a pinned inference runtime on local hardware,
records everything that could make the result comparable, and publishes it to localmax.net.
"""

__version__ = "0.1.0"
SCHEMA_VERSION = "1.0.0"
